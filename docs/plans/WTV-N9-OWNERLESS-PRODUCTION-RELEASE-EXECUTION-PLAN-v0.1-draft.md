# WTV-N9-OWNERLESS-PRODUCTION-RELEASE-EXECUTION-PLAN v0.1-draft

## Document status

- Plan ID: `WTV-N9-OWNERLESS-PRODUCTION-RELEASE-EXECUTION-PLAN-v0.1`
- Status: `DRAFT / STAGE 1 TECHNICALLY COMPLETE / CANONICAL RE-ADOPTION PENDING / NOT EXECUTION AUTHORIZED`
- Primary owner／sole writer: Tech Lead
- Decision owner: Human
- Governing repository Head at drafting: `693a3acbdc85e71541f559ac8fce2a484b3db538`
- Current application source-delta classification: `N9_SOURCE_DELTA_ZERO_CONFIRMED`
- Current helper state: `COMPLETE / STAGE 0 ACCEPTED`
- Current Stage 0 state: `N9_STAGE_0_COMPLETE_ZERO_DELTA / HUMAN ACCEPTED`
- Current Stage 1 state: `N9_STAGE_1_COMPLETE_ZERO_SOURCE_DELTA / TECHNICALLY COMPLETE / CANONICAL RE-ADOPTION PENDING`
- Current GraphQL state: `OUT OF SCOPE / NOT REQUIRED`
- Current Stage 2 state: `NOT STARTED / NOT AUTHORIZED`
- Current Production N9 execution: `NOT STARTED / NOT AUTHORIZED`
- Permissions generated by this candidate: canonical re-adoption review only
- Permissions not generated: Contract／Plan re-adoption itself、Stage 2 Git integration、implementation、QA outside a bounded validation scope、Operation Packet drafting、credential operation、provider operation、Production SQL／migration、deployment、Firewall、Data API toggle、smoke、cleanup、merge、N9 closeout

This Execution Plan defines minimum stages、dependencies、authority boundaries and Operation Packet grouping. It is not a runbook、SQL body、provider click procedure、Operation Packet or execution authority.

## 1. Governing authority and current state

Use these exact authorities:

1. [N9 requirements §3.13](../03_requirements.md#313-n9-ownerless-production-release), SHA-256 `03baf7af067c8a8d6a655d727f659446edf49e2289655e19a53f5fc9bdc159df`
2. [N9 DoD §3.8](../05_dod.md#38-n9-ownerless-production-releasenot-execution-authorized), SHA-256 `a0e1ad8ee3810b742db7435d711932b136dabc9f1b3917bd7eedd76919b2c7e8`
3. [N9 QA §2.5](../06_qa-flow.md#25-n9-ownerless-production-release-qa-contract), SHA-256 `d8a9c2176a7af3fd7efc369ccf0ba2d722a36ed0ec029a6a5bc28bd387236ab9`
4. [Final QA-authority-synced N9 Execution Contract](../contracts/WTV-N9-OWNERLESS-PRODUCTION-RELEASE-EXECUTION-CONTRACT-v0.1-draft.md), SHA-256 `a81f74fba2bbab147168307cfee073473c81b948295dddc6924d7ac3a17f9f48`, lifecycle `QA-AUTHORITY-SYNC CANDIDATE / NOT RE-ADOPTED`

Drafting provenance is distinct from the current governing-candidate lifecycle:

- predecessor Contract `WTV-N9-OWNERLESS-PRODUCTION-RELEASE-EXECUTION-CONTRACT-v0.1`, SHA-256 `41c811e1e21414458ee408aa797b724c867f81793705f08b80a5ba674334a400`, lifecycle `HUMAN ADOPTED`;
- separate Human drafting authorization: `N9_CONTRACT_TERMINOLOGY_AND_EXECUTION_PLAN_DRAFTING`;
- the identity-synchronized Contract's `NOT EXECUTION-PLAN AUTHORIZED` status generates no permission from that candidate itself; this separate authorization permits drafting and review only; and
- the identity-synchronized Contract must be Human re-adopted after identity sync before the synchronized Execution Plan is re-adopted. Joint review generates neither Contract nor Plan re-adoption.

Current entry facts are planning inputs, not permission or guaranteed currentness:

- N8 is `CLOSED` at published Head `693a3acbdc85e71541f559ac8fce2a484b3db538`;
- remote main／current Production application commit is `87295a19f80192ffbe91c56dded86748d3a51bbd`;
- Supabase Production ref is `ehmivhmsnhcrynvuahaq` and QA ref is `twcbycyyrxbovtgiqaun`;
- Production Data API is OFF and QA Data API is ON;
- exact 8 business tables are zero in both environments at the accepted baseline;
- Production ownerless target is absent and creator route is not deployed;
- QA M11 ownerless shape has been observed; and
- application service-role use is zero.

Fresh evidence in Stage 0 or a later authorized Packet controls mutable facts. QA evidence is not Production proof.

Stage 1 completion is based on the accepted Local／QA evidence already recorded: isolated N9 runtime、N6 delta 0、creator path、`/api/events`、M01–M11、M12 absent、catalog／RLS、pgTAP 118、N6 history 20/20、N7 429 10/10、regression、check／build、QA target／profiles、Preview→QA binding、positive／negative REST／Data API、creator route、fixture cleanup and final exact 8 tables zero. Stage 1 is not rerun in this sync.

## 2. Objective and planning principles

Move from the N8 maintenance state to N9 internal Production acceptance using the smallest safe execution chain.

Apply these principles:

- **Integration-first:** M11、creator route、N6 history and N7 client 429 handling already exist. Application source delta is zero by default unless fresh inventory proves a concrete incompatibility. `N9_SOURCE_DELTA_ZERO_CONFIRMED` means zero new N9 product behavior、Production ownerless migration body and N9 architecture delta; it does not deny the tracked isolation／runtime-routing／creator-profile tooling、tests or operational documentation used to complete Stage 1. Stage 2 classifies those deltas and commit scope from fresh inventory.
- **Minimum stages:** combine work only when owner、environment、risk and STOP／rollback boundary align. Separate distinct mutations、credential handling、destructive cleanup and ambiguous-outcome boundaries.
- **Plan／Packet separation:** this Plan defines purpose、dependency、owner、environment、permitted scope、completion evidence、Human gate and routing. A Packet defines exact target、operation、artifact、count、Human action、retry and read-back.
- **No implicit permission:** Plan adoption does not authorize any stage. Every execution scope uses the separate authority below.
- **No automatic progression:** a dependency arrow or prior PASS never authorizes the next operation.

## 3. Stage model

### Stage 0 — Current-state and execution readiness

**Purpose:** establish fresh identities and decide whether source delta remains zero.

**Current status:** `N9_STAGE_0_COMPLETE_ZERO_DELTA / HUMAN ACCEPTED`. This stage is complete and is not rerun by this sync.

**Permitted candidate work after separate authorization:** repository／Git topology reads、GitHub／Vercel／Supabase Connector inventory、QA／Production target separation、Vercel metadata-gap classification and the bounded QA security-catalog SELECT-only read already required by QA §2.5.

**No Production mutation. A later QA validation scope, if required, is authorized once for the bounded scope and is not split into ordinary per-read／per-mutation gates.**

**Completion evidence:**

- governing artifact identities and owner-known dirty scope fixed;
- current QA effective security catalog known without M11 reapplication;
- current provider／environment state classified;
- exact remaining source／configuration gaps known;
- blocking identity contradiction zero; and
- source delta is exactly one of `N9_SOURCE_DELTA_ZERO_CONFIRMED`、`N9_SOURCE_DELTA_BOUNDED_REQUIRED` or `N9_SOURCE_DELTA_UNKNOWN`.

`N9_SOURCE_DELTA_BOUNDED_REQUIRED` requires an exact change scope and separate Human implementation authorization. `N9_SOURCE_DELTA_UNKNOWN` blocks Stage 1 implementation and Stage 2 Head fixation.

### Stage 1 — Local／QA release validation

**Purpose:** prove the cumulative source candidate and current QA state before Git integration.

**Current status:** `N9_STAGE_1_COMPLETE_ZERO_SOURCE_DELTA / TECHNICALLY COMPLETE / CANONICAL RE-ADOPTION PENDING`. Existing evidence is accepted; Local／QA execution is not rerun in this sync.

**Permitted candidate work after separate authorization:** isolated Local clean-chain replay、role／grant／policy negative tests、route response matrix、N6 history、N7 client 429 behavior、QA Data API／REST／collaboration、Preview→QA binding and exact QA fixture cleanup. GraphQL is `OUT OF SCOPE / NOT REQUIRED` for N9.

QA M11 reapplication is zero. Source changes are allowed only after Stage 0 returns `N9_SOURCE_DELTA_BOUNDED_REQUIRED` and Human authorizes the exact implementation scope.

**Completion evidence:** Local／QA DoD evidence complete、QA fixtures zero、blocking review finding zero and a technically fixed release candidate. Local PASS does not prove QA or Production. The completed evidence is summarized above; GraphQL is `OUT OF SCOPE / NOT REQUIRED`.

### Stage 2 — Cumulative release Head and Git integration

**Purpose:** fix the exact N9 release Head and the identities used by Production artifacts.

Fresh Git evidence must decide:

- exact base and current stacked topology;
- PR #42 current role;
- whether the existing N8 branch remains the cumulative release line or a dedicated N9 branch is required;
- how Requirements／DoD／QA／Contract／Plan and any authorized source delta join the release Head; and
- exact commit／push／Draft／Ready authority and Human merge decision point.

Do not choose a branch strategy by assumption. Do not merge automatically.

**Completion evidence:** exact release Head and migration identity fixed、checks and required reviews PASS、publication disposition known. Production operation artifacts may be finalized only after this identity is fixed.

### Stage 3 — Production isolation and schema transition

**Purpose:** establish external isolation and migrate exact Production to ownerless state while Data API remains OFF.

This single planning stage contains distinct bounded authorities:

1. Vercel Authentication current-state proof and Human enable only if OFF;
2. exact Production target／Data API OFF／fresh 8×0 preflight;
3. Human SQL Editor execution of exact M11; and
4. post-migration catalog／ACL／row-count verification.

Authentication isolation precedes creator-route activation and other write-path activation. Exact 8-table zero is fresh immediately before migration. Production SQL follows `operate-supabase-live-db`; no Agent substitutes for Human SQL Editor.

**Completion evidence:** ownerless catalog present、old-owner artifacts absent、creator role password null、effective ACL postcheck PASS、exact 8 tables zero、retry 0 and unresolved outcome zero.

### Stage 4 — Credential、deployment and Firewall activation

**Purpose:** activate the exact cumulative application and route-specific abuse protection without conflating distinct mutations.

Distinct Human authorities remain required for creator credential provision、Production environment binding、deployment and Firewall mutation／activation. A PASS does not authorize the next operation.

Required dependency order:

1. creator credential provision changes secret classification only and does not change role privilege、password以外のrole attributeまたはmembership;
2. Production-only environment bindingのpre-activation相関を行い、service-role binding absentを確認する;
3. target-correlated fresh 8×0をdeployment activation直前の必須gateとして確認し、nonzeroまたはtarget mismatchならactivation 0でSTOPする;
4. exact cumulative Head deployment activation and `READY`／alias／binding read-back;
5. Firewall active／draft／versions inventory、unknown draft zero、exact intended diff、draft mutation、activation and active read-back; and
6. controlled 429 plus pre-Data-API creator-route readiness／negative evidence.

Draft or PATCH success is not active enforcement.

**Completion evidence:** exact deployment READY、Production→Production binding、Authentication ON、exact POST rule active、unrelated semantic delta zero、controlled 429 PASS、rejected business-row delta zero and creator route ready for Data API activation.

### Stage 5 — Data API activation and internal acceptance

**Purpose:** restore Data API and perform the minimum correlated Production acceptance.

Data API ON requires a separate Human operation after Stage 4 PASS. Toggle outcome and smoke authority remain separate even if grouped in one Packet family.

After Dashboard ON read-back, evaluate REST／Data API. Production fault injection、full response-matrix execution and full collaboration regression are zero. GraphQL is `OUT OF SCOPE / NOT REQUIRED` for N9.

The bounded smoke scope is:

- one exact correlated Event＋default Criterion through `/api/events`;
- share-page lookup and one representative collaboration path;
- one REST／Data API negative;
- direct Event INSERT and creator negative-permission checks;
- N6 successful lookup history; Stage 4のcontrolled 429 evidenceがContractのN7 client 429 coverageを満たし、Stage 5での追加429 operationは0; and
- unexpected row zero outside the correlated fixture.

**Completion evidence:** Data API ON、authorized paths PASS、prohibited paths denied、exact fixture graph known、retry zero and unresolved outcome zero.

### Stage 6 — Cleanup and N9 closeout

**Purpose:** remove only the correlated fixture and produce the final technical and Human disposition.

Destructive cleanup is Human-only and requires exact fixture correlation、reviewed artifact、operation count and retry zero. Non-correlated rows are never cleanup candidates.

**Completion evidence:** exact 8 tables zero、ownerless state retained、Data API ON、Authentication ON、Firewall active、unexpected drift zero、known pre-existing／provider-managed variance separately classified with required effective denial、compact evidence complete and technical `N9_COMPLETE` classification.

Human N9 closeout acceptance remains separate. Public launch、N12、N13 and merge are not automatically authorized.

## 4. Critical dependency graph

```text
Terminology-aligned Contract adoption + Execution Plan adoption
→ Stage 0 readiness and source-delta decision
→ Stage 1 Local／QA PASS
→ Stage 2 exact release Head
→ Authentication isolation
→ Production M11
→ Production security postcheck
→ creator credential／binding／deployment
→ Firewall active
→ Data API ON
→ internal smoke
→ exact cleanup
→ technical N9_COMPLETE
→ separate Human N9 closeout acceptance
```

Arrows express prerequisites only. They do not grant the next read、mutation or execution permission.

## 5. Authority matrix

| Stage | Environment | Primary owner | Agent-autonomous scope after exact authorization | Human decision／operation | Packet family |
|---|---|---|---|---|---|
| 0 | Repository／Connectors／Supabase QA | Tech Lead＋Supabase specialist | reads、inventory、evidence preparation | completed; future bounded QA validation scope if required | A |
| 1 | Local／Supabase QA／Preview | Full-stack＋Supabase specialist | Local validation、QA read／mutation／fixture／cleanup within one bounded validation scope、bounded source correction if separately required | bounded validation scope; source implementation only if separately required | A＋authorized implementation scope |
| 2 | GitHub／repository | Tech Lead | read-only topology、authorized branch-local Git workflow | publication／Ready／merge disposition | Plan-bound Git authority; no provider Packet |
| 3 | Vercel Production／Supabase Production／Human SQL Editor | DevOps＋Supabase specialist | authorized reads and evidence review only | Authentication mutation、M11 execution | B＋C |
| 4 | Vercel Production | DevOps | authorized reads and evidence review only | credential、environment、deployment、Firewall operations | D＋E |
| 5 | Supabase／Vercel Production | DevOps＋Full-stack | authorized read-only verification and evidence review | Data API ON、Production smoke | F |
| 6 | Supabase Production／canonical docs | Tech Lead＋Supabase specialist＋PKA | authorized postcheck／evidence／docs scope | cleanup COMMIT、N9 closeout | G |

Role assignment and matrix placement do not generate authority.

## 6. Operation Packet grouping

Use seven minimum-sufficient families because each has a distinct target、risk or outcome boundary. Family count may change only if the adopted Plan correction proves authority is preserved.

### A — QA read／validation

Bounded QA validation scope: security-catalog read、secret-free Vercel binding confirmation、current-state evidence、normal task-correlated fixture／REST／Data API／permission checks and cleanup may be grouped when same target、risk and validation goal align. QA-wide reset、credential／provider changes and ownership-unknown cleanup remain Human return conditions.

### B — Production access isolation

Authentication current-state proof、Human enable if OFF、ON read-back and external denial. Ambiguous outcome stops all later Production work.

### C — Production migration

Exact target／8×0 preflight、exact M11 artifact、Human SQL Editor execution and post-migration read-back under `operate-supabase-live-db`.

### D — Production creator activation

Coordinates but separately authorizes credential provision、Vercel Production binding、deployment and post-deploy correlation. It never records credential values and requires evidence that credential provision changed no role privilege、password以外のrole attributeまたはmembership.

### E — Production Firewall

Inventory、draft、activation、active read-back and controlled 429. Draft mutation and activation are distinct operations.

### F — Data API and internal acceptance

Coordinates Data API ON read-back and minimal smoke but keeps toggle authorization and smoke authorization separate. If one artifact cannot preserve the two decisions, the family is split without changing this Plan.

### G — Cleanup and final postcheck

Exact correlated fixture cleanup、postcheck and closeout evidence. Cleanup COMMIT remains Human-only and cannot be inferred from discovery or ROLLBACK evidence.

Packet adoption never authorizes execution. Exact Packet count is finalized only after Stage 0／2 facts are known.

## 7. Evidence model

Each stage records only:

- evidence identity;
- exact environment／target correlation;
- operation count;
- retry count;
- terminal verdict; and
- next separate Human decision.

Evidence is compact and secret-free. Do not retain raw terminal dumps、raw business rows、raw share pathname、credential values、CA content、password verifier or raw function body. A missing、truncated or uncorrelated result is not PASS.

## 8. STOP and recovery routing

Every stage uses `PASS`、`STOP` or `OUTCOME_UNKNOWN`. Known failure does not automatically authorize retry. `OUTCOME_UNKNOWN` prohibits blind repeat and next-stage progression.

| Condition | Stop at | Return boundary |
|---|---|---|
| source incompatibility／source-delta unknown | Stage 0／1 | exact inventory and possible bounded implementation reauthorization |
| Git base／Head／artifact drift | Stage 2 | Git strategy／publication Human decision |
| Production target、preflight、M11 or postcheck mismatch | Stage 3 | migration Packet／Human diagnosis |
| credential、binding、deployment or service-role anomaly | Stage 4 | creator activation boundary |
| Authentication ineffective or ambiguous | Stage 3 | access-isolation Packet／Human |
| Firewall unknown draft、inactive rule or activation ambiguity | Stage 4 | Firewall Packet／Human |
| Data API toggle／REST ambiguity | Stage 5 | Data API Packet／Human |
| unexpected／non-correlated Production row | current Production stage | stop all later mutations and correlate evidence |
| cleanup correlation／COMMIT／postcheck ambiguity | Stage 6 | cleanup Packet／Human; no repeat |
| Contract／requirements contradiction | affected stage | Human re-adoption; no local reinterpretation |

Recovery preserves current state、actual operation counts and secret-free evidence. Do not substitute providers、expand scope or convert an unknown result to success／not applied.

## 9. Git integration strategy decision

Stage 0 obtains fresh GitHub Connector and repository evidence. Stage 2 then fixes exactly one strategy:

- continue the current stacked release line from the proven cumulative base; or
- create a dedicated N9 release branch from the exact Human-approved base.

The decision must account for PR #42 current role、N8 branch state、current requirements／DoD／QA／Contract／Plan files、any bounded source delta and the final cumulative ancestry. No branch、commit、push、PR、Ready or merge operation is authorized by this candidate.

## 10. Source-delta decision

Stage 0 must emit exactly one:

- `N9_SOURCE_DELTA_ZERO_CONFIRMED`: current application source already satisfies N9; no new N9 product behavior、Production ownerless migration body or N9 architecture delta is required. Stage 1 task-created tracked tooling／tests／operational documentation may still exist and must be inventoried separately at Stage 2.
- `N9_SOURCE_DELTA_BOUNDED_REQUIRED`: a concrete incompatibility has an exact source／test scope and requires separate Human implementation authorization.
- `N9_SOURCE_DELTA_UNKNOWN`: evidence is insufficient; implementation and release Head fixation stop.

Documentation／release-integration delta is recorded separately from application source delta. Do not invent source work to progress the Plan.

## 11. Human decision map

The following decisions remain separate:

1. terminology-aligned Contract review
2. terminology-aligned Contract re-adoption
3. Execution Plan review
4. Execution Plan adoption
5. Git publication／release Head workflow
6. one bounded QA validation scope authorization (normal QA read／mutation／fixture／cleanup are not micro-gated)
7. bounded source implementation, only if required
8. Vercel Authentication mutation
9. Production migration
10. creator credential provision
11. Vercel binding／deployment
12. Firewall mutation／activation
13. Data API ON
14. Production smoke
15. cleanup COMMIT
16. N9 technical evidence review and closeout acceptance
17. merge, if applicable

Review moments may be coordinated, but underlying mutation permissions are never combined or inherited.

## 12. Explicit non-scope

- public launch or Authentication解除;
- N12／N13;
- user migration、backfill、dual-write、drain or owner compatibility;
- Data API OFF replacement routes;
- full ACL provenance or provider ACL normalization;
- broad Production regression;
- generic deployment／evidence／orchestration framework;
- unrelated cleanup; and
- advertising、SEO or public-index activation.

## 13. Plan completion and routing

This candidate is Human-review-ready only when:

- aligned Contract identity is fixed;
- stages、dependency graph、authority matrix、Packet families、source-delta classification、Git decision point、STOP routing and Human decisions are complete;
- governing Requirements／DoD／QA identities are fixed and referenced exactly;
- required read-only reviewers report P0／P1／blocking P2 equal to zero; and
- exact two task-owned paths and common validation pass.

Execution Plan review or adoption does not authorize Stage 0、Packet drafting、Git publication、implementation、QA／Production operation or closeout.

Current lifecycle is `STAGE 1 TECHNICALLY COMPLETE / CANONICAL RE-ADOPTION PENDING`. Human re-adoption order is Requirements → DoD／QA → Execution Contract → Execution Plan. After all four layers are re-adopted, the next Human decision is:

`N9_STAGE_2_GIT_INTEGRATION_AUTHORIZATION`

Re-adoption does not authorize Stage 2, Git publication, Production operation or closeout. No later stage starts automatically.
