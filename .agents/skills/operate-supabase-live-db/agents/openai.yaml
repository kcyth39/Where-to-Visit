interface:
  display_name: "Supabase Migration Operations"
  short_description: "Gate local replay, live migrations, E2E, Git, and cleanup"
  default_prompt: "Use $operate-supabase-live-db to run safe local and remote Where-to-Visit Supabase gates."
